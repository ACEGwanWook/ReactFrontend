# ── 서버 설정 ──────────────────────────────────────────────
server.port=8090

# ── MySQL DB 연결 (기존 프로젝트와 동일하게 입력하세요) ────────
spring.datasource.url=jdbc:mysql://localhost:3306/hmd_weld_robot_v2?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ── JPA (ScheduleEvent 엔티티 전용) ───────────────────────────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ── InfluxDB (사용 시에만 설정) ──────────────────────────────
influx.url=http://localhost:8086
influx.token=NumvEpeYkc_7BxD90Hy1l3jbnaXf0mDc_mw9pmUgDyOg3hUO8hw_Rb_1efJn2kNxi4bXDMi_rIgW7uKhXJFIaQ==
influx.org=ACE
influx.bucket=row_data

# ── CORS (React 개발서버 허용) ──────────────────────────────
# WebConfig.java 에서 설정합니다.
# 운영 배포 시에는 allowed-origins를 실제 도메인으로 변경하세요.

# ── 로깅 ────────────────────────────────────────────────────
logging.level.com.example.uiproject=DEBUG
spring.jackson.mapper.accept-case-insensitive-properties=true
